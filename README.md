# ISA-Simulator
ISA I3xM is a processor emulator inspired by a subset of the RISC-V architecture, implemented in C, which simulates instruction execution, register and memory management, and provides a terminal-based interface for visualization and interaction.
